require('dotenv').config(); const { createClient } = require('@libsql/client'); (async()=>{const c=createClient({url:process.env.TURSO_DATABASE_URL,authToken:process.env.TURSO_AUTH_TOKEN});const r=await c.execute('SELECT id, title, video_data, broker_creci, images FROM properties');r.rows.forEach(p=>{const hasVideo=!!(p.video_data);const imgCount=p.images?JSON.parse(p.images).length:0;console.log(p.id+' | '+p.title+' | video:'+hasVideo+' | images:'+imgCount+' | broker:'+p.broker_creci)});process.exit(0)})();
